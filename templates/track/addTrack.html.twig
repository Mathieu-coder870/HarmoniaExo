{% extends 'base.html.twig' %}

{% block title %}Hello ContactController!
{% endblock %}

{% block body %}


	{{form_start(form)}}

	{{form_row(form.title)}}
	{{form_row(form.duration)}}
	{{form_row(form.numberTrack)}}

	<button type="submit">Valider</button>
	{{form_end(form)}};

{% endblock %}
