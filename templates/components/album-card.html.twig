<div class="card" style="width: 18rem;">
	<img src="{{asset(album.pouch)}}" class="card-img-top" alt="...">
	<div class="card-body">
		<h5 class="card-title">{{album.title}}</h5>
		<p class="card-text">durée de l'album :

		</p>
		<p class="card-text">
			Nombre de pistes:

		</p>
		<a href="{{ path('app_album_id', {'id' : album.id}) }}" class="btn btn-primary">Lien de l'album</a>
	</div>
</div>
